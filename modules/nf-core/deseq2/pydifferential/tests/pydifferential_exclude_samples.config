process {
    withName: 'DESEQ2_PYDIFFERENTIAL' {
        ext.args = { "--round_digits 4 --blocking_variables $meta.blocking --vs_method vst --exclude_samples_col sample_number --exclude_samples_values sample1" }
    }
}
